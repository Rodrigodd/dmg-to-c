(cell
  dmg_xnor
  (inputs in1 in2)

  (outputs y)

  (registers )

  (assignments

    (y
      (xnor in1 in2)
      (+ (elmore (wire 122) (* (nmos 35) 2)) (elmore (wire L_y) (* (pmos 35) 2)))
    )
  )
)
