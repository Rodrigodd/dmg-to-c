(cell
  sm83_nand2_a
  (inputs in1 in2)

  (outputs y)

  (registers)

  (parameters (L_y 14))

  (assignments

    (y (nand in1 in2) (delay (elmore (wire L_y) (pmos 3)) (elmore (wire L_y) (* (nmos 3) 2))))
  )
)
