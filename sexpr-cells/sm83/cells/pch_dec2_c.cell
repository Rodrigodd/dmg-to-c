(cell
  sm83_pch_dec2_c
  (inputs pch_n)

  (outputs y)

  (registers)

  (parameters (L_y 0))

  (assignments

    (y
      (bufif0-zs 1 pch_n)
      (delay (elmore (wire L_y) (pmos 7)) (elmore (wire L_y) (pmos 7)) (elmore (wire L_y) (pmos 7)))
    )
  )
)
