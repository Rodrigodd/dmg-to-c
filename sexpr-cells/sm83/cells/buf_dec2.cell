(cell
  sm83_buf_dec2
  (inputs in)

  (outputs y)

  (registers)

  (assignments

    (y
      in
      (delay
        (+ (elmore (wire 25) (nmos 8)) (elmore (wire L_y) (pmos 34)))
        (+ (elmore (wire 25) (pmos 6)) (elmore (wire L_y) (nmos 8)))
      )
    )
  )
)
