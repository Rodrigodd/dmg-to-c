(cell
  sm83_and2_irq
  (inputs in1 in2)

  (outputs y)

  (registers)

  (assignments

    (y
      (and in1 in2)
      (delay
        (+ (elmore (wire 35) (* (nmos 3) 2)) (elmore (wire L_y) (pmos 14)))
        (+ (elmore (wire 35) (pmos 3)) (elmore (wire L_y) (nmos 6)))
      )
    )
  )
)
