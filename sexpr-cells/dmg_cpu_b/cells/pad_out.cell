(cell
  dmg_pad_out
  (inputs o_n)

  (outputs pad)

  (registers)

  (parameters (L_pad 0))

  (assignments

    (pad (not o_n) (delay (elmore (wire L_pad) (pmos 140)) (elmore (wire L_pad) (nmos 140))))
  )
)
