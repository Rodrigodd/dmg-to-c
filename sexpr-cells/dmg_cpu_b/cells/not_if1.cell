(cell
  dmg_not_if1
  (inputs in ena)

  (outputs y)

  (registers)

  (parameters (L_y 54))

  (assignments

    (t0 (not in) (delay 0))
    (d0
      ena
      (delay
        (elmore (wire 153) (nmos 35))
        (+ (elmore (wire 153) (nmos 35)) (elmore (wire 90) (pmos 35)))
        0
      )
    )
    (y
      (bufif1 t0 d0)
      (delay
        (elmore (wire L_y) (* (pmos 35) 2))
        (elmore (wire L_y) (* (nmos 35) 2))
        (+
          (elmore (wire 153) (pmos 35))
          (elmore (wire 90) (nmos 35))
          (elmore (wire L_y) (* (pmos 35) 2))
        )
      )
    )
  )
)
