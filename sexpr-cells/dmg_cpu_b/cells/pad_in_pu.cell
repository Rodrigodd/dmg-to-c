(cell
  dmg_pad_in_pu
  (inputs pad)

  (outputs i_n pad)

  (registers)

  (parameters (L_i_n 160) (L_pad 0))

  (assignments

    (pad (drive-zp 1) (delay 0))
    (i_n (not pad) (delay (elmore (wire L_i_n) (pmos 15)) (elmore (wire L_i_n) (nmos 7))))
  )
)
