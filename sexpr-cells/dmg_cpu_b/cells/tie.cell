(cell
  dmg_tie
  (inputs)

  (outputs gnd vdd)

  (registers)

  (parameters (L_gnd 0) (L_vdd 0))

  (assignments

    (gnd (drive-strength 0 supply1 supply0) (delay 0))
    (vdd (drive-strength 1 supply1 supply0) (delay 0))
  )
)
